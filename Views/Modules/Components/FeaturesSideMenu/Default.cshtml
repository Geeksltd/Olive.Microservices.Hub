@model ViewModel.FeaturesSideMenu
@(Layout = null)

<!-- «Start| FeaturesSideMenu» -->
<div data-module="FeaturesSideMenu" class="features-side-menu">
    @Html.StartupActionsJson()
    @if (User.Identity?.IsAuthenticated == true)
    {
        @info.Markup.Raw()
    }
</div>
<!-- «End| FeaturesSideMenu» -->

