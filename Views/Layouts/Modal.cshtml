@{
    Layout = Request.IsAjaxRequest() ? null : "Modal.Container";
}
<main class="hub-service">
    <div class="container-fluid">
        <div class="page">
            <div class="content"> @RenderBody() </div>
        </div>
    </div>

    @RenderSection("scripts", required: false)
    @Html.RegisterStartupActions()
</main>